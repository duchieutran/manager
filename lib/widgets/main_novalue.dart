import 'package:flutter/material.dart';

class MainNovalue extends StatelessWidget {
  const MainNovalue({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("No value ! "),
    );
  }
}
